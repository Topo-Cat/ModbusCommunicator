# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always normalize and convert to LF
# These files will be normalized to LF in the repository
*.cs text eol=lf
*.xaml text eol=lf
*.xml text eol=lf
*.json text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.csproj text eol=lf
*.sln text eol=lf
*.config text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf

# Declare files that will always have CRLF line endings on checkout.
# These are less common for source code but might be needed for specific Windows-only files.
# *.bat text eol=crlf
# *.cmd text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.dll binary
*.exe binary
*.pdb binary
*.nupkg binary
# ... (add other binary file types specific to your project)
